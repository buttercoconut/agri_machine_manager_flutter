name: agri_machine_manager
description: A Flutter app for managing agricultural machines and maintenance.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.5
  sqflite: ^2.2.0+3
  path_provider: ^2.0.15
  intl: ^0.18.0
  flutter_local_notifications: ^12.0.4
  # Add other dependencies as needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
